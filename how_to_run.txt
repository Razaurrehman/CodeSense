════════════════════════════════════════════════════════════════
  CodeSense — How to Run
════════════════════════════════════════════════════════════════

STEP 1 — Configure Environment
────────────────────────────────
  cd ~/Desktop/codesense
  cp .env.example .env

  Edit .env and set all five values:

    GIT_TOKEN=your_fine_grained_pat
    GITHUB_CLIENT_ID=your_oauth_client_id
    GITHUB_CLIENT_SECRET=your_oauth_client_secret
    GROQ_API_KEY=your_groq_api_key
    OLLAMA_MODEL=codellama:7b

  GitHub OAuth App (if you don't have one):
  → https://github.com/settings/developers → OAuth Apps → New

    Homepage URL  :  http://localhost:5174
    Callback URL  :  http://localhost:8080/api/v1/auth/callback

  Fine-grained GitHub PAT (if you don't have one):
  → https://github.com/settings/tokens?type=beta → Generate new token
  Required permissions: Contents: Read, Pull requests: Read, Metadata: Read

  Groq API key (free, ~2-5s responses):
  → https://console.groq.com → API Keys → Create new key


STEP 2 — Start Backend Services
────────────────────────────────
  sg docker -c "docker compose up -d"

  This starts:
    • CodeSense API   →  http://localhost:8080
    • arq Worker      →  (internal — processes queued scan jobs)
    • Ollama          →  http://localhost:11434
    • ChromaDB        →  http://localhost:8000
    • PostgreSQL      →  localhost:5433  (remapped from 5432)
    • Redis           →  localhost:6380  (remapped from 6379)

  NOTE: Use "sg docker -c" prefix for all docker commands.
  This is required because the docker group needs a session restart to take effect.


STEP 3 — Pull Ollama Models (first time only)
──────────────────────────────────────────────
  Fallback LLM (used when Groq rate-limits):

    sg docker -c "docker exec codesense-ollama ollama pull codellama:7b"

  Required for semantic search (always pull this):

    sg docker -c "docker exec codesense-ollama ollama pull nomic-embed-text"


STEP 4 — Start the UI
──────────────────────
  cd ~/Desktop/codesense/frontend
  npm install        # first time only
  npm run dev

  UI runs on http://localhost:5174


STEP 5 — Open & Use
────────────────────
  Web UI     →  http://localhost:5174
  API docs   →  http://localhost:8080/docs
  Health     →  http://localhost:8080/api/v1/health

  Running a single workflow scan:
    1. Go to http://localhost:5174 and log in with GitHub
    2. Click a workflow in the sidebar (e.g. Bug Scan)
    3. Click "+ Add Scan" (top right)
    4. Select a repository from the dropdown
    5. For PR Review: also select an open pull request from the list
    6. Click "Run Scan" — the job is queued via Redis/arq
    7. Table refreshes every 6 seconds; click "Analytics" when done

  Running a full multi-scan batch:
    1. Click "Repo Scans" in the sidebar
    2. Click "+ Scan Repository"
    3. Select a repo and one or more scan types
    4. Click "Run Scan"
    5. Download the PDF report when status shows "Completed"

  Viewing results:
    • Analytics button → charts (findings by type, scan status pie) + scan results table
    • View Findings button → right-side drawer with bullet-point findings + raw output
    • Download PDF → full formatted report for all scan types in the job


════════════════════════════════════════════════════════════════
  USEFUL COMMANDS
════════════════════════════════════════════════════════════════

  Live backend logs:
    sg docker -c "docker compose logs -f app"

  Live worker logs (scan job processing):
    sg docker -c "docker compose logs -f worker"

  Restart app + worker:
    sg docker -c "docker compose up -d --force-recreate app worker"

  Stop everything:
    sg docker -c "docker compose down"

  Health check:
    curl http://localhost:8080/api/v1/health

  List all scan jobs:
    curl http://localhost:8080/api/v1/jobs

  Check a specific job by UUID:
    curl http://localhost:8080/api/v1/jobs/by-uuid/<uuid>


════════════════════════════════════════════════════════════════
  PATCHING BACKEND CODE (without full rebuild)
════════════════════════════════════════════════════════════════

  Use this workflow to apply Python file changes fast:

    sg docker -c "docker run -d --name cs_patch --entrypoint sleep codesense-app 3600"
    sg docker -c "docker cp <local-file> cs_patch:/app/<container-path>"
    sg docker -c "docker commit cs_patch codesense-app && docker rm -f cs_patch"
    sg docker -c "docker compose up -d --force-recreate app worker"

  Example — patch pdf_gen.py:
    sg docker -c "docker cp app/worker/pdf_gen.py cs_patch:/app/app/worker/pdf_gen.py"

  Frontend changes are picked up automatically by Vite HMR (no restart needed).


════════════════════════════════════════════════════════════════
  DATABASE MIGRATIONS
════════════════════════════════════════════════════════════════

  SQLAlchemy create_all does NOT alter existing tables.
  For new columns, run ALTER TABLE directly:

    sg docker -c "docker exec codesense-postgres psql -U codeagent -d codeagent -c \
      'ALTER TABLE scan_jobs ADD COLUMN IF NOT EXISTS <col> <type>;'"

  Columns added since initial setup:
    uuid       VARCHAR(36)  UNIQUE  DEFAULT gen_random_uuid()::text
    pr_number  INTEGER


════════════════════════════════════════════════════════════════
  HOW IT WORKS
════════════════════════════════════════════════════════════════

  LLM:
    Primary  →  Groq API (llama-3.3-70b-versatile) — ~2-5s per task
    Fallback →  Ollama local (codellama:7b) — used on Groq 429 / rate limit

  Groq free tier limits:
    1,000 requests/day · 30 req/min · 6,000 tokens/min · 100,000 tokens/day

  Job queue:
    Backend  →  Redis + arq worker
    Max concurrent jobs: 4
    Retries on failure: 2
    PDF generated automatically on job completion (stored in reports_data volume)
    PDF also generated on-demand at download time if missing

  All 11 workflows are queue-based — select a repo and click Add Scan:

    PR Review      →  two-step: pick repo → pick specific open PR → review that PR's diff
    Bug Scan       →  per-chunk scan across full repo (80-line chunks, max 25)
    Explain Code   →  architecture overview: layers, modules, data flow, entry points
    Refactor       →  identifies refactoring opportunities with priority + code snippets
    Similar Bugs   →  finds recurring bug patterns appearing in 2+ locations
    Generate Tests →  auto-detects framework, generates runnable test code
    Migration Plan →  phased strangler-fig plan with effort estimates
    Impact Analysis →  blast-radius map of high-impact symbols + hotspot files
    Version Bumps  →  reads manifests, recommends specific version numbers
    License Check  →  audits LICENSE + manifests for copyleft compliance risks
    Vuln Scan      →  CVE detection across all dependency manifests

  Repo Scans (batch mode):
    Select multiple scan types for one repo → single queued job → one PDF covering all types


════════════════════════════════════════════════════════════════
  Built by Raza Ur Rehman · SocialPie Engineering · 2026
════════════════════════════════════════════════════════════════
